// Doop!

var Width = 800;
var Height = 400;
var framerate = 60;

var font1 = createFont("Times New Roman");

void setup(){
    size(Width, Height);
    frameRate(framerate);
};

void draw(){
    background(255,255,255);
};
